import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  barraProgress: {
    alignItems: 'flex-start',
    paddingStart: 30,
    paddingTop: 40,
    backgroundColor: '#fff',
  },
});
